import React from 'react'

const RangeSlider: React.FC = () => {
  return <div 
    css={css`
      color: #ece9fb;
      &:hover,
      &:active {
        color: #6f3ed1;
      }
      `}>touch area</div>
}

export default RangeSlider
